---
applications:
  - name: static-btp-app
    memory: 64M
    disk_quota: 128M
    instances: 1
    buildpacks:
      - staticfile_buildpack
    path: .
    no-route: true          # no public URL — only reachable via App Router
    services:
      - xsuaa-static-app
